/**
 * @file Risk.js
 * 리스크 판정 타입 — RiskEngine 출력 계약
 */

/**
 * @typedef {Object} RiskVerdict
 * @property {boolean} allowed
 * @property {string[]} reasons
 * @property {number} [maxPositionKrw]
 * @property {number} [cooldownMs]
 */

module.exports = {};
