/**
 * Re-export for lib/strategy/edge/* and lib/strategy/* (require('../../config.default') → lib/config.default)
 * 실제 설정은 dashboard/config.default.js
 */
module.exports = require('../config.default');
